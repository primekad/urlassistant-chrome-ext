# URL Assistant Chrome Extension

A very simple chrome extension to save page urls

## Installation

Install the extension in chrome by cloning this repository and loading folder as an unpacked extension in chrome.
